const {chromium}=require('C:/Users/didou/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const assert=require('node:assert/strict');
(async()=>{const browser=await chromium.launch({headless:true,channel:"chrome"});const page=await browser.newPage({viewport:{width:1440,height:1000}});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://127.0.0.1:8000');await page.waitForSelector('.discovery-card');assert.equal(await page.locator('.discovery-card').count(),12);await page.locator('#decouvrir').scrollIntoViewIfNeeded();await page.screenshot({path:'artifacts/desktop.png'});await page.locator('[data-discovery="iles"]').click();assert.equal(await page.locator('.discovery-card').count(),4);await page.locator('[data-discover-open="nosy-iranja"]').first().click();assert.equal(await page.locator('#sheet').isVisible(),true);await page.locator('[data-sheet-add]').click();assert.equal(await page.locator('#tripCount').innerText(),'1');await page.keyboard.press('Escape');await page.reload();assert.equal(await page.locator('#tripCount').innerText(),'1');await page.locator('#q').fill('ramena');assert.equal(await page.locator('#tripDrawer').isVisible(),false);assert.equal(await page.locator('#sheet').isVisible(),false);await page.locator('#themeBtn').click();assert.equal(await page.locator('html').getAttribute('data-theme'),'dark');await page.screenshot({path:'artifacts/dark.png'});await page.setViewportSize({width:390,height:844});await page.locator('#decouvrir').scrollIntoViewIfNeeded();await page.screenshot({path:'artifacts/mobile.png'});assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth),false);await page.locator('[data-discover-open="allee-baobabs"]').first().click();await page.screenshot({path:'artifacts/mobile-detail.png'});await page.keyboard.press('Escape');await page.evaluate(async()=>{for(const img of document.images){img.loading='eager';}await Promise.all([...document.images].map(i=>i.decode().catch(()=>{})))});assert.equal(await page.locator('#discoveryGrid img').evaluateAll(imgs=>imgs.filter(i=>!i.naturalWidth).length),0);assert.deepEqual(errors,[]);console.log('PASS: 12 cards, filters, detail, save/persistence, search shortcuts, theme, mobile overflow, images, no JS errors');await browser.close()})().catch(e=>{console.error(e);process.exit(1)});


